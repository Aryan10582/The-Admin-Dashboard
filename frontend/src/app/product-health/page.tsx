import { AppShell } from "@/components/layout/AppShell";

export default function ProductHealthPage() {
  return (
    <AppShell>
      <h1 className="text-2xl font-semibold">Product Health</h1>
      <p className="mt-2 text-sm text-muted-foreground">Product health monitoring is intentionally deferred.</p>
    </AppShell>
  );
}
