import { AppShell } from "@/components/layout/AppShell";

export default function DashboardPage() {
  return (
    <AppShell>
      <div>
        <h1 className="text-2xl font-semibold">Admin Dashboard Foundation Ready</h1>
        <p className="mt-2 text-sm text-muted-foreground">Core auth, navigation, and API foundations are ready for feature work.</p>
      </div>
    </AppShell>
  );
}
