import { AppShell } from "@/components/layout/AppShell";

export default function ProductsPage() {
  return (
    <AppShell>
      <h1 className="text-2xl font-semibold">Products</h1>
      <p className="mt-2 text-sm text-muted-foreground">Product deployment management will be added in a later phase.</p>
    </AppShell>
  );
}
