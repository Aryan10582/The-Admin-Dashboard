class BillingService:
    """Future home for credit balance, dues, plan assignment, and ledger calculations."""

    def calculate_future_balance(self) -> None:
        raise NotImplementedError("Billing calculations are intentionally deferred to a later phase.")
